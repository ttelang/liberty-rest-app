# Server Bootstrap Properties
# These properties are loaded at a very early stage of the server startup process

# Application context root
app.context.root=/inventory

# Default HTTP port
default.http.port=7050

# Default HTTPS port
default.https.port=7051

# Application name
app.name=inventory
