# Server Bootstrap Properties
# These properties are loaded at a very early stage of the server startup process

# Application context root
app.context.root=/order

# Default HTTP port
default.http.port=8050

# Default HTTPS port
default.https.port=8051

# Application name
app.name=order
