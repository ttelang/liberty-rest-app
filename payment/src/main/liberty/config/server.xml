<?xml version="1.0" encoding="UTF-8"?>
<server description="Payment Service">
    <featureManager>
        <feature>jakartaee-10.0</feature>
        <feature>microProfile-6.1</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint" httpPort="9050" httpsPort="9443" />

    <webApplication location="payment.war" contextRoot="/payment" />

    <!-- Define a basic user registry with a single user -->
    <basicRegistry id="basic" realm="BasicRealm">
        <user name="admin" password="adminpwd" />
    </basicRegistry>

    <!-- Log to stdout in JSON format -->
    <logging traceSpecification="com.ibm.ws.microprofile.*=all" 
             consoleLogLevel="INFO" 
             consoleFormat="json" 
             consoleSource="message,trace,accessLog,ffdc" />

    <!-- Define unique instance ID -->
    <variable name="server.name" value="PaymentService" />
    
    <!-- Application information -->
    <applicationManager autoExpand="true" />
    
    <!-- Health check endpoint configurations -->
    <mpHealth />
</server>
